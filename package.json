{
  "name": "mongoose-property-sync",
  "version": "1.0.0",
  "description": "Keeps properties of different mongoose collections in sync.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "repository": "https://github.com/alex-paterson/mongoose-property-sync.git",
  "dependencies": {
    "mongoose": "^5.0.0-rc2"
  },
  "devDependencies": {
    "eslint": "^4.14.0"
  }
}
